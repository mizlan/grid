{
    "files": [
        "src/"
    ],
    "scripts": {
        "start": "parcel serve src/index.html",
        "build": "node_modules/.bin/tsc && cross-env NODE_ENV=production parcel build src/index.html --public-url ."
    },
	"license": "UNLICENSED",
    "devDependencies": {
        "cross-env": "^7.0.2",
        "gh-pages": "^3.1.0",
        "parcel-bundler": "^1.9.7",
        "typescript": "^4.0.2"
    }
}
